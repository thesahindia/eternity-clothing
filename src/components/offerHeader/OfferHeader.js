import React from 'react'
import "./offerHeader.scss"
export default function OfferHeader({children}) {
    return (
        <div className="offer-header">
            {children}
        </div>
    )
}
